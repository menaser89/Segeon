#!/usr/bin/env node
var fs = require('fs'),
    path = require('path'),
    args = process.argv.slice(2),
    config;

if (args[0]) {
  config = JSON.parse(fs.readFileSync(args[0]).toString());
}

require('./thorax-inspector.js')(config);